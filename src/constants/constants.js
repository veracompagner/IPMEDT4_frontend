// Dev URL for php artisan serve:
export const APIURL = "http://" + window.location.hostname + ":8000";
// Production URL for apache2:
// export const APIURL = "https://api." + window.location.hostname;
